<Window
    x:Class="Banking_app.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Banking_app"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="WomBank Login"
    Width="500"
    Height="300"
    Icon="/Wombat_Bank.ico"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    Background="#0B1F3A"
    mc:Ignorable="d">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Titel -->
        <TextBlock
            Margin="0,0,0,25"
            HorizontalAlignment="Center"
            FontSize="22"
            FontWeight="Bold"
            Foreground="White"
            Text="Anmeldung" />

        <!-- Benutzername -->
        <StackPanel Grid.Row="1">
            <TextBlock Foreground="White" Text="Benutzername" />
            <TextBox
                x:Name="txtUser"
                Height="25"
                Margin="0,5,0,15"
                Background="#F2F6FF"
                BorderBrush="#3B82F6" />
        </StackPanel>

        <!-- Passwort -->
        <StackPanel Grid.Row="2">
            <TextBlock Foreground="White" Text="Passwort" />
            <PasswordBox
                x:Name="txtPassword"
                Height="25"
                Margin="0,5,0,25"
                Background="#F2F6FF"
                BorderBrush="#3B82F6" />
        </StackPanel>

        <!-- Login Button -->
        <Button
            Grid.Row="3"
            Height="35"
            Click="Login_Click"
            Content="Anmelden"
            IsDefault="True"
            Background="#1E3A8A"
            Foreground="White"
            BorderBrush="#1E3A8A"/>
    </Grid>
</Window>